

const initialState={
    places:[],
    selectPlace:null
    
};
const reducerName = (state = initialState, action) => {
    switch (action.type) {
        case 'ACTION_TYPE':
            return 
        default:
            return state
    }
}

export default root;